# docs/ — index

One line per file. Every doc starts with YAML frontmatter (`status: plan|active|reference` + one-line `description`); adding/removing a doc means updating this index too. Rules + periodic GC: the `docs-gc` skill (`.claude/skills/docs-gc/`, `check.sh` = mechanical lint). A `plan` that fully ships gets deleted — git history is the archive.

## Active (living docs, updated as work proceeds)

_(none yet)_

## Plans (forward work, not fully executed)

_(none yet)_

## Reference (stable records, runbooks, decided analyses)

_(none yet)_
